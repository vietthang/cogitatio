{
  "name": "@cogitatio/json",
  "version": "0.0.4-alpha.5",
  "main": "dist/index.js",
  "types": "dist/packages/src/index.d.ts",
  "scripts": {
    "clean": "rimraf ./dist",
    "prebuild": "yarn clean",
    "build": "tsdx build --tsconfig tsconfig.build.json --entry src/index.ts"
  },
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "base64-arraybuffer": "^0.2.0"
  }
}
