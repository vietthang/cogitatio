{
  "name": "@cogitatio/nestjs-logger",
  "version": "0.0.12-alpha.0",
  "main": "dist/index.js",
  "types": "dist/packages/nestjs-logger/src/index.d.ts",
  "scripts": {
    "clean": "rimraf ./dist",
    "prebuild": "yarn clean",
    "build": "tsdx build --target node --tsconfig tsconfig.build.json --entry src/index.ts",
    "prepublishOnly": "yarn build",
    "test": "jest"
  },
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "files": [
    "/src",
    "/dist"
  ],
  "dependencies": {
    "winston": "^3.2.1"
  },
  "peerDependencies": {
    "@nestjs-utils/common": "0.0.1-alpha.0"
  },
  "devDependencies": {
    "@nestjs/testing": "^6.10.11"
  }
}
